using EntityLayer.Concrete;
using FluentValidation;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.Intrinsics.X86;
using System.Text;
using System.Threading.Tasks;

namespace BusinessLayer.ValidationRules
{
	public class WriterValidator : AbstractValidator<Writer>
	{
  //      public WriterValidator()
  //      {
		//	RuleFor(x => x.WriterMail).NotEmpty().WithMessage("Mail kısmı boş geçilemez.");

		//	RuleFor(x => x.WriterName).NotEmpty().WithMessage("İsim kısmı boş geçilemez.");
  //          RuleFor(x => x.WriterName).MinimumLength(2).WithMessage("İsiminiz en az 2 karakter olmalıdır.");
  //          RuleFor(x => x.WriterName).MaximumLength(30).WithMessage("İsminiz en fazla 30 karakter olabilir.");

		//	RuleFor(x => x.WriterPassword).NotEmpty().WithMessage("Şifre kısmı boş geçilemez.");
		//	RuleFor(x => x.WriterPassword).Matches("[A-Z]").WithMessage("Şifre en az 1 büyük harf içermelidir.");
		//	RuleFor(x => x.WriterPassword).Matches("[a-z]").WithMessage("Şifre en az 1 küçük harf içermelidir.");
		//	RuleFor(x => x.WriterPassword).Matches("[0-9]").WithMessage("Şifre en az bir rakam içermelidir.");

		//}
     
    }
}
